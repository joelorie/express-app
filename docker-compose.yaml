name: "express-app"

services:
  watchtower:
    image: nickfedor/watchtower
    command: --label-enable
    environment:
      WATCHTOWER_POLL_INTERVAL: 60
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    container_name: watchtower

  node-app:
    image: middleofjune/express-app:latest
    ports: 
      - 8080:8080
    container_name: node-app
